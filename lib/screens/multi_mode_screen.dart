import 'package:flutter/material.dart';

// . . . ? ? ? . . .

class MultiModeGameScreen extends StatefulWidget {
  @override
  State<StatefulWidget> createState() {
    return MultiModeGameScreenState();
  }
}

class MultiModeGameScreenState extends State<MultiModeGameScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
