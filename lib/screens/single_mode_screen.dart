import 'package:flutter/material.dart';

// SinglePayer have some mode [easy_mode, normal_mode, hard_mode]
// [hard_mode] - with timer (100 sec)
// [normal mode] - timer (200 sec)
// [easy mode] - no timer

class SinglePlayerScreen extends StatefulWidget {
  @override
  State createState() {
    return SinglePlayerScreenState();
  }
}

class SinglePlayerScreenState extends State<SinglePlayerScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
