import 'package:flutter/material.dart';

//Text (rules of the game with an explanation of how to play)
//and Button (Skip - for first start)

class HowToPlay extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
